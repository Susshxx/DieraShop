// NEW badge component for recently added products
const NewBadge = () => (
  <div className="absolute top-3 left-3 z-10">
    <span className="text-foreground text-sm font-bold tracking-wide">NEW</span>
  </div>
);

export default NewBadge;
